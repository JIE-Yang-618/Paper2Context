# Contributing

Contributions are welcome. Keep the project deterministic-first and local-first.

1. Create a focused branch.
2. Add or update tests for extraction behavior.
3. Run `pytest` and `ruff check .`.
4. Avoid introducing required cloud, LLM, vector-database, or telemetry dependencies.
5. Document any heuristic that can produce uncertain output.
