# Data model

The core model contains diagnostics, metadata, sections, paragraphs, references, assets, chunks, and warnings. Paragraphs retain `page_start`, `page_end`, and section names. Chunks are created by scholarly section boundaries rather than blind fixed-size splitting.
