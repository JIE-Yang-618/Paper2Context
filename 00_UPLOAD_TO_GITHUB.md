# Upload to GitHub

Create an empty **Public** repository named `Paper2Context`.

Recommended description:

> Turn academic PDFs into provenance-rich, AI-ready research context packs. Local-first and no LLM required.

When creating the repository, keep GitHub's README, .gitignore, and license generators OFF because this package already contains them.

Recommended topics:

`pdf` `research` `academic-papers` `llm` `context-engineering` `local-first` `python` `markdown` `research-tools` `ai-agents`

Upload the *contents* of this folder so `README.md` appears at repository root. Do not upload the release ZIP as the repository contents.
